class TreeNode {
  private value: number;
  // private children: [TreeNode, TreeNode];

  public constructor(value: number) {
    this.value = value;
    // this.children = new Array<TreeNode>(2);
  }

};

export default TreeNode;